export interface MoviesInter {
  imdbID:string;       // Adjust to 'number' or 'string' depending on your API
  Title: string;
  Poster: string;
  Year:string;
  Type:string;
  }

 export interface Params {
    id: number;
  }